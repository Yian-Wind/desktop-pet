# 待办

## 已完成

1. 修复向下方、左下方拖动后不触发掉落动画的问题。
2. 长按松手掉落后增加小幅结构弹性回弹，并恢复 idle 状态。
3. 修改冲浪动画：跳上冲浪板，向右滑出窗口，从左侧滑回原地，原地跳下冲浪板并回到待机状态。
4. 支持 Fairy / Mualani 各自独立保存与切换大小比例。
5. 点击动画可被拖动打断，并在进入拖动时重置点击动画残留状态。
6. 新增 `举手张嘴` 动画（`> <` 眼、张嘴、双手举起），并在设置中提供临时测试按钮。
7. 鼠标靠近宠物时显示左侧环形快捷按钮，支持今日待办推荐与定时闹钟。
8. 气泡固定在宠物上方不遮挡，显示时长可在设置中调整。
9. 快捷按钮支持左侧环形排列、动态触发区域、闹钟输入和连续交互打断。
10. 对于包括点击、拖动，等等涉及随机的部分改为伪随机。

## 待排期
1. 修复 Fairy 快捷按钮的触发与显示逻辑问题。
2. 优化冲浪滑行末尾姿态与原地跳下冲浪板姿态之间的连续性。滑行末尾速度和滑出速度匹配，滑行末尾速度加速（优先级低）
3. 表情动画后续打磨：（打瞌睡与 `> <` 张嘴举手已实现；`> <` 闭嘴沿用拖动表情，不单独制作）
   - 打瞌睡头发异步飘动，修正当前头发动作方向。
4. 物理化pet，实现拖动时晃动，高阻尼。
5. 接入 API：待办按钮基于自定义筛选原则的 prompt，对各 Base 中的待办条目智能选择，并以角色特点回复；支持注入不同上下文内容。（初版已实现，待测试）
6. 加快启动速度
7. 让闹钟更加明显（优先级高，比如系统通知）