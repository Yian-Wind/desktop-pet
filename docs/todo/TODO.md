# 待办

## 已完成

1. 修复向下方、左下方拖动后不触发掉落动画的问题。
2. 长按松手掉落后增加小幅结构弹性回弹，并恢复 idle 状态。
3. 修改冲浪动画：跳上冲浪板，向右滑出窗口，从左侧滑回原地，原地跳下冲浪板并回到待机状态。
4. 支持 Fairy / Mualani 各自独立保存与切换大小比例。

## 待排期

1. 确认 `点点头` 随机概率是否偏低；必要时调整点击动画概率权重。
   1. 对于包括点击、拖动，等等涉及随机的部分改为伪随机。
2. 优化冲浪滑行末尾姿态与原地跳下冲浪板姿态之间的连续性。滑行末尾速度和滑出速度匹配，滑行末尾速度加速（优先级低）
3. 整理新的 Mualani 眼部表情素材，绘制以下预设表情：（`> <` 拖动表情已接入；剩余打瞌睡、闭嘴、张嘴预设待整理）
   - 闭眼微微点头、打瞌睡。————优先级较高，供给长时间待机表情变化
   - `> <` 眼睛、嘴巴闭上。
   - `> <` 眼睛、嘴巴张开。
4. 鼠标靠近时显示快捷按钮：
   - 今日待办推荐：点击后推荐今日待办，使用 `> <` 睁嘴表情，双手各斜向上 45°；后续决定 API 或无 API 兼容方案。
   - 定时闹钟：点击后提示设置完成，使用 `> <` 闭嘴表情，无需 API。
5. 物理化pet，实现拖动时晃动，高阻尼。
6. 待办注入API，对应需要按钮实现。
7. 点击动画可以被拖动打断，以免出现眼睛渲染bug加快
8. 加快启动速度
9. 气泡位于pet上方不要遮挡，气泡存在时间可设置缩短
10. 接入API，实现对待办的快捷梳理

